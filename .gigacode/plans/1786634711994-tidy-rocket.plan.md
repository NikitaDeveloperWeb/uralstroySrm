# План рефакторинга: `app/projects/[id]/page.tsx`

## Проблема
Файл содержит ~1100 строк с дублирующимся кодом и встроенными UI-компонентами. При этом в `shared/components/projects/` **уже есть 13 компонентов**, которые **не используются** детальную страницу.

## Найденные извлечённые компоненты (не используются)

| Компонент | Назначение |
|-----------|-----------|
| `ProjectDetailHeader.tsx` | Заголовок + бригада + статус |
| `ProjectDetailInfo.tsx` | Сетка: тип, площадь, стоимость, дата, сложность, адрес |
| `ProjectDetailPrepayment.tsx` | Предоплата с прогресс-баром |
| `ProjectDetailLabor.tsx` | Таблица оплаты труда |
| `ProjectDetailManager.tsx` | Оплата менеджменту |
| `EditForm.tsx` | Базовая форма редактирования (без карты объекта) |
| `ReportModal.tsx` | Модалка отчёта |
| `projectDetailUtils.ts` | Утилиты, константы, форматтеры |
| `EditableTable.tsx` | Редактируемая таблица |
| `PaymentTable.tsx` | Таблица категорий оплаты |
| `ManagerPayment.tsx` | Компонент оплаты менеджера |
| `TransactionTable.tsx` | Таблица транзакций |
| `TemplatePickerModal.tsx` | Выбор шаблона |

## Шаги рефакторинга

### Шаг 1: Подключить существующие компоненты (min changes)

Заменить inline-код на использование уже извлечённых компонентов:

**1.1 Заменить header-секцию**
- `shared/components/projects/ProjectDetailHeader.tsx` — уже имеет весь header + бригаду + статус
- **Изменения в компоненте:** добавить prop `onStatusChange`, чтобы статусные кнопки вызывали коллбек

**1.2 Заменить инфо-сетку**
- `shared/components/projects/ProjectDetailInfo.tsx` — полная замена 6 `InfoItem` блоков

**1.3 Заменить предоплату**
- `shared/components/projects/ProjectDetailPrepayment.tsx` — полная замена блока предоплаты

**1.4 Заменить блок оплаты труда**
- `shared/components/projects/ProjectDetailLabor.tsx` — уже содержит PaymentTable + ManagerPayment
- **Изменения в компоненте:** поднять state статусов в `useProjectLaborStore` (Zustand), вынести константы категорий в props

**1.5 Заменить модалки**
- `EditForm.tsx` — заменить inline edit modal (только базовые поля)
- `ReportModal.tsx` — заменить inline report modal
- **Новый `ProjectObjectCardEditModal.tsx`** — модалка для редактирования карты объекта (этажи, крыша, фундамент и т.д.)

**1.6 Добавить извлечённый компонент: `ProjectOverheads.tsx`**
- Вынести в отдельный компонент таблицу overheads с inline-редактированием, кнопками шаблонов, сохранением

**1.7 Добавить извлечённый компонент: `ProjectEstimateSection.tsx`**
- Секция с кнопками "Смета материалов", "Смета работ", "Общие расходы", "Назначить бригаду"

### Шаг 2: Извлечь оставшиеся inline-компоненты

**2.1 `ProjectObjectCard.tsx`** (новый)
- Секция "Карта объекта" с collapsible-секцией
- Все поля: этажи, крыша, фундамент, стены, утепление, окна, двери, коммуникации и т.д.
- Группировать поля по категориям (структура, материалы, опции)

**2.2 `ProjectObjectCardEditModal.tsx`** (новый)
- Модалка для редактирования карты объекта (вместо inline в EditForm)
- Чекбоксы: мансарда, веранда, крыльцо
- Conditional: размер веранды

### Шаг 3: Очистка дубликатов иdead code

**3.1 Удалить дубликаты полей в отображении карты**
- `baseType` и `homeType` отображаются дважды (строки ~520-540)

**3.2 Consolidate константы в `projectDetailUtils.ts`**
- `complexityColors`, `statusColors`, `typeIcons` в файле уже есть → заменить inline-объявления на импорты
- `defaultPaymentCategories` и `banyaPaymentCategories` → вынести в `projectDetailUtils.ts`
- `managerPercentage` → уже есть как `MANAGER_PERCENTAGE`

**3.3 Удалить неиспользуемые интерфейсы**
- `MaterialEstimate`, `WorkEstimate`, `TemplateItem`, `OverheadTemplateItem` — можно переиспользовать типы из stores

### Шаг 4: Сократить логика business logic

**4.1 Consolidate fetch-логика**
- `fetchOverheads`, `fetchOverheadTemplates`, `fetchBrigades` — перенести в useEffect-хук или store actions

**4.2 Simplify handleMaterialsSave / handleWorkSave**
- 2 функции очень похожи → создать generic helper `saveEstimateItems<T>(items, createUrl, updateUrl, deleteUrl, refetch)`

**4.3 Создать Zustand store для оплаты труда**
- `shared/stores/projectLaborStore.ts` — state для статусов оплаты (paymentStatuses)
- Actions: `toggleStatus`, `resetStatuses`

### Итоговая структура

```
app/projects/[id]/page.tsx          # ~300 строк (было ~1100)
shared/components/projects/
├── ProjectDetailHeader.tsx         # ✅ уже есть, minor updates
├── ProjectDetailInfo.tsx           # ✅ уже есть, minor updates
├── ProjectDetailPrepayment.tsx     # ✅ уже есть, minor updates
├── ProjectDetailLabor.tsx          # ✅ уже есть, minor updates (Zustand)
├── ProjectDetailManager.tsx        # ✅ уже есть, minor updates
├── EditForm.tsx                    # ✅ уже есть, base fields only
├── ReportModal.tsx                 # ✅ уже есть, minor updates
├── ProjectOverheads.tsx            # 🆕 extracted from detail page
├── ProjectEstimateSection.tsx      # 🆕 extracted buttons section
├── ProjectObjectCard.tsx           # 🆕 extracted map card display
├── ProjectObjectCardEditModal.tsx  # 🆕 extracted map card edit form
└── projectDetailUtils.ts           # ✅ уже есть, extend with payment categories
```

### Файлы для модификации

| Файл | Действие |
|------|----------|
| `app/projects/[id]/page.tsx` | Удаление inline-компонентов, подключение извлечённых |
| `shared/components/projects/ProjectDetailHeader.tsx` | Добавить prop `onStatusChange` |
| `shared/components/projects/ProjectDetailLabor.tsx` | Обновить типы статусов, сделать категории props |
| `shared/components/projects/EditForm.tsx` | Только базовые поля (без карты) |
| `shared/components/projects/projectDetailUtils.ts` | Добавить `defaultPaymentCategories`, `banyaPaymentCategories` |
| `shared/stores/projectLaborStore.ts` | 🆕 Zustand store для статусов оплаты |
| `shared/components/projects/ProjectOverheads.tsx` | 🆕 создать |
| `shared/components/projects/ProjectEstimateSection.tsx` | 🆕 создать |
| `shared/components/projects/ProjectObjectCard.tsx` | 🆕 создать |
| `shared/components/projects/ProjectObjectCardEditModal.tsx` | 🆕 создать |

### Верификация
1. Открыть `/projects/[id]` — все секции отображаются корректно
2. Изменить статус — работает
3. Сменить бригаду — работает
4. Редактировать материалы/работы/расходы — CRUD работает
5. Collapsible-секции — сворачиваются/разворачиваются
6. Запустить `npm run build` — без ошибок
