# План: Восстановление и улучшение сметы в модальных окнах

## Проблема
- `app/projects/[id]/page.tsx` сломан после частичных правок (дубли функций, остаточный код)
- Нужно восстановить оригинальную функциональность + добавить улучшения

## План восстановления

### Шаг 1: Восстановить page.tsx
- Полностью перепisać страницу с нуля
- Сохранить всю оригинальную логику (редактирование проекта, статус, оплата труда и т.д.)
- Добавить поддержку поля `category` в модалках смет
- Перейти от прямого `fetch` к использованию `materialEstimateStore` для операций с материалами/работами

### Шаг 2: Обновить EstimateTable
- Добавить поддержку `category` в таблицу
- Добавить кнопку "Экспорт PDF" вместо "Печать"
- Интегрировать `exportEstimateToPDF` для экспорта материалов/работ
- Добавить разбивку по категориям в футер

### Шаг 3: Обновить MaterialsTab и WorksTab
- Удалить (не нужны при модалках)
- Перенести логику в `page.tsx` как состояние модалок

### Шаг 4: Обновить модалки смет в page.tsx
- Добавить отображение категории в таблицу
- Добавить кнопку "Экспорт PDF"
- Подключить `exportEstimateToPDF`
- Использовать `materialEstimateStore` вместо прямых fetch

### Шаг 5: Тестирование
- Запустить dev сервер
- Проверить загрузку проекта
- Проверить модалки материалов и работ
- Проверить добавление из шаблонов
- Проверить PDF экспорт
- Проверить сохранение изменений

## Файлы для изменения
1. `app/projects/[id]/page.tsx` — восстановить + улучшить
2. `shared/components/estimates/EstimateTable.tsx` — добавить PDF export и category
3. `app/projects/[id]/estimate/MaterialsTab.tsx` — удалить
4. `app/projects/[id]/estimate/WorksTab.tsx` — удалить
5. `app/projects/[id]/estimate/page.tsx` — удалить

## Приоритеты
1. Критично: Восстановить page.tsx
2. Высокий: Добавить PDF export и category в модалки
3. Низкий: Удаление лишних файлов
