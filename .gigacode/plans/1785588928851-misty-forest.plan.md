# План: Улучшение приложения (пункты 4, 7, 8, 10, 11)

## 1. Связь проектов и бригад (пункт 4)

### Файлы:
- `shared/data/projects.ts` — добавить поле `brigadeId` в интерфейс `Project`
- `shared/data/brigades.ts` (НОВЫЙ) — вынестиbrigades в общие данные
- `app/projects/page.tsx` — в форме редактирования объекта добавить select бригад
- `app/projects/[id]/page.tsx` — добавить секцию "Бригада" с инфой о назначенной бригаде

## 2. Уведомления (пункт 7)

### Файлы:
- `shared/components/navigation/NotificationBell.tsx` (НОВЫЙ) — компонент колокольчика с бейджем
- `shared/data/notifications.ts` (НОВЫЙ) — логика генерации уведомлений (просроченные проекты, низкий остаток)
- `shared/components/navigation/NavMenu.tsx` — добавить колокольчик рядом с логотипом

**Типы уведомлений:**
- Просроченные проекты (дата сдачи < сегодня, статус != "завершен")
- Критически мало на складе (status === "критически мало")
- Нет в наличии на складе (status === "нет в наличии")

## 3. Больше начальных данных (пункт 8)

### Файлы:
- `shared/data/projects.ts` — добавить ещё 10 проектов с разными датами, статусами
- `shared/data/employees.ts` (НОВЫЙ) — расширить список сотрудников до 30+
- `shared/data/brigades.ts` — расширить до 5 бригад
- `shared/data/warehouse.ts` (НОВЫЙ) — добавить больше складских позиций

## 4. Фильтрация календаря проектов (пункт 10)

### Файлы:
- `shared/components/projects/ProjectsCalendar.tsx` — добавить фильтры по статусу и типу объекта

## 5. Загруженность бригад на календаре (пункт 11)

### Файлы:
- `shared/components/projects/ProjectsCalendar.tsx` — отображать полосы/метки бригад на датах проектов

---

## Детальная реализация

### Шаг 1: shared/data/brigades.ts (НОВЫЙ)
```ts
export interface Brigade {
  id: number;
  name: string;
  leaderId: number;
  memberIds: number[];
  skills: string[];
}

export const initialBrigades: Brigade[] = [...]; // 5 бригад
```

### Шаг 2: shared/data/projects.ts — добавить `brigadeId: number | null`

### Шаг 3: shared/data/notifications.ts (НОВЫЙ)
Функция `getNotifications(projects, warehouseItems)` возвращает массив:
```ts
interface Notification {
  id: number;
  type: 'overdue' | 'low-stock' | 'out-of-stock';
  message: string;
  priority: 'high' | 'medium';
  link?: string;
}
```

### Шаг 4: NotificationBell.tsx (НОВЫЙ)
- Компонент колокольчика в header навигации
- Бейдж с количеством уведомлений
- Dropdown при клике со списком уведомлений

### Шаг 5: Обновить NavMenu.tsx — добавить NotificationBell

### Шаг 6: Обновить shared/data/projects.ts — добавить 10+ проектов

### Шаг 7: Обновить shared/data/brigades.ts — добавить 5 бригад

### Шаг 8: Обновить app/projects/page.tsx — добавить select бригады в форму редактирования

### Шаг 9: Обновить app/projects/[id]/page.tsx — показать бригаду на странице проекта

### Шаг 10: Обновить ProjectsCalendar.tsx — фильтры и загруженность бригад

## Проверка
- `npm run build` — без ошибок
- На странице проектов в форме редактирования есть выбор бригады
- На странице проекта отображается назначенная бригада
- Колокольчик в навигации показывает уведомления
- Календарь фильтруется по статусу/типу
- В календаре отображаются бригады
